# 秒单位修正版

版本 `contract-v1-seconds-20260909`，用户在2026-09-09明确以 `end_time-begin_time` 输出秒数，修正此前推断的整数毫秒。赛题4继续使用 `result/result.json` 与单键对象 `result/infer_time.json`，不采用赛题1另输出result.json的示例；字段沿一致写法 `infer_time`。

## 本版变化及边界

- 330条原始结果逐字节保留，SHA256为 `854ff00d40f2dc7234dc9d5aa64a9388b212ab4ac4a4905529e921bb5d0ded37`。
- 计时从原始 `entry_timing.json` 取得 `6194.539663076401` 秒，写入 `{"infer_time":6194.539663076401}`，约103.24分钟。它仍来自上一轮缓存复用运行，未以单位修正伪装加速或新入口重跑。
- 包装入口在 `import time` 后立即记录开始时间，包含后续导入、准备、加载、实际推理、结果校验及结果发布准备；结果准备结束时取结束时间，直接保存秒差。计时JSON本身的序列化及后续收尾位于结束采样之后。恢复只记录当前调用，停机间隔不累计。
- 新入口通过秒数保真、结束采样位置、非法值拒绝、来源变化拒绝及旧结果保留检查；完整模型未在新入口重跑。当前发布的是历史结果的单位修正版，不能作为新入口完整冷启动速度。
- 模型、输入和21个核心依赖不变；报告/评级仍为串行调用，107条病害各一次。89.93分钟报告阶段占原入口87.10%，并发优化不在本次变更内。

## 交付及恢复

官方快照位于 `/workspace/work/c4-submission-versions/contract-v1-seconds-20260909/submit`，压缩包为同目录上一级的 `c4-contract-v1-seconds-20260909.tar.gz`。模型依赖见 `code/model_manifest.json`，精确文件及结果摘要见 `package_manifest.json`。

GitHub固定标签为 `c4-contract-v1-seconds-20260909`。旧版 `c4-contract-v1-20260909` 和对应官方快照保留，不改写旧版毫秒记录。受限逐图结果仅在官方工作区，仓库仅保存代码、设计书和聚合元数据。本版未比赛上传或评测。
