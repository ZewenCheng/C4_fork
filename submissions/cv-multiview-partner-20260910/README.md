# 五折多视图分类与有限伙伴复核版

平台完整推理与固定包独立验收通过：330条七字段结果、330次伙伴复核、54个提交文件。固定GitHub标签为 `c4-cv-multiview-partner-20260910`；正式平台切换以前须完成该标签及逐文件字节的独立读回。未提交比赛评测，暂无新分数。

## 行为变化

多视图ConvNeXt分类头取代旧融合首选作为本版锁定分类源。区域候选筛选、规范检索、结构位置推断及条件评级输入同步使用该类型；旧融合、局部视觉、检测、分割和异常模型保留为证据。伙伴可有限补证和提出建议，未验证的二次改判保持关闭。

病害描述由确定性事实模板产生；Qwen执行条件评级和伙伴内部复核，内部理由不直接作为描述。桥名与位置继续采用metadata2语义，不由分类目录冒充桥名。

## 选择证据及适用边界

用户授权本地初赛训练集，3402个文件去重为3178份内容、3403条标注；平台逐字节及逐内容标注绑定核验一致。桥梁按实体隔离、轨道按内容隔离，固定五折三路并发。全图2295/3178、多视图2300/3178，新增16、误改11、净增5，四折正一折负。此为类型标签集合Top1命中，未证明统计显著性，不代表相对原生产融合或七字段赛事提分。

最终多视图头在全部3178份内容重新拟合，70类，模型SHA256 `d676b6cb23d85842994c04ab06f7e360620b64671cccf9ddca37b395488367d2`。没有选取单折模型，也未用训练准确率充当独立成绩。线上330图与该训练内容交集为0；公开预训练交叉和轨道采集序列独立性未知。

## 运行与恢复

`code/run` 是统一入口；默认读取官方线上测试目录并在独立work运行，成对写入官方结果目录。显式 `--work`、`--output` 可隔离候选，`--resume` 核验配置、输入及产物后恢复。每次调用的推理计时由完整脚本开头到结果准备结束的 `time.time()` 秒差产生；完成恢复保留原计时，不将恢复时间称全量冷启动。

本次生成经历两次失败调用及一次显式兼容修复。前两次入口实测分别为12016.792406082153秒和2044.6374771595001秒：第一次基线超时，第二次主推理完成后，伙伴初始化错误拒绝了已登记的训练资产符号链接。修复保留训练来源摘要校验，并按准备凭证限定外部资产绑定；4项平台路径回归及真实双融合头初始化、单案件重算通过。迁移完整保留335份产物，包含330条已生成报告；没有重新选择模型、改输入或修改既有主结果。

提交包计时采用上述两次与最终成功调用的入口实测秒之和，由独立账本复算。停机等待、修复代码与独立验收时间不计入；内部阶段耗时及并行权重预读不重复相加。原生结果、原生计时、失败凭证和迁移前后快照均保留在平台。该口径是经过已核验兼容迁移的累计执行时间，不是同一冻结源码的一次冷启动耗时。

新增模型原位置 `/workspace/work/c4-cv-multiview-final-20260910`，独立备份 `/workspace/work/c4-cv-multiview-final-backup-20260910`。`multiview_model.npz`、`manifest.json` 和 `complete.json` 须按摘要一起恢复。其他外置模型见 `code/model_manifest.json`。模型和逐图预测不进入公开仓库。

平台完整运行目录 `/workspace/work/c4-cv-partner-release-20260910-v2/inference`。正式冻结目录预定 `/workspace/work/c4-submission-versions/cv-multiview-partner-20260910`。源码身份以逐文件清单、整清单摘要及运行配置为准；继承的历史源包字段只指祖先包，不代表本版源码摘要。

整链74项平台回归及输入隔离预检通过；修复4项平台测试及真实双头初始化通过。最终330条七字段、分类来源和桥名校验通过，完成恢复22.991秒且结果/计时摘要不变。伙伴330次调用无过程错误，282条维持、48条建议均未采用。相对metadata2：病害类型54条、位置31条、描述57条、评级39条变化；桥名、文件名及赛题类别均不变。变化数不等于正确数。

提交 `infer_time` 为 **24650.22799181938秒（410.837133分钟）**；末次成功调用原生计时为10588.798108577728秒（176.479968分钟）。前两次调用、兼容迁移和计时复算见 `evidence/timing-ledger-21a387f25f0b7d67.json`。

固定压缩包SHA256：`a464362abc02789ec7adfedf92d3a228ec979c12b47c2ce84a44487f8b5e5f96`；包清单SHA256：`152b90b157ba99d841cce4319a69dd664e97bdc2d4f4a1b99ed25bbe8899a5f4`。平台压缩包位于 `/workspace/work/c4-submission-versions/cv-multiview-partner-20260910/c4-cv-multiview-partner-20260910.tar.gz`。包内模型清单、结果摘要、入口自检和二次独立读回通过。
