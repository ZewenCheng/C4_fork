# C4 源码仓库交付规则

本目录遵循上级 C4 项目规则。日志、报告、版本说明默认使用中文。

用户已要求：每个拟正式提交的版本必须先备份到 `ZewenCheng/C4_fork`。必要的版本提交、向该仓库非强制推送、新建固定标签已获持续授权，不重复询问；比赛提交或评测仍需另有授权。

- 正式提交前，按 `submissions/README.md` 固定代码、配置、设计书、模型版本与清单、结果摘要、运行记录和官方工作区原始制品快照。
- 每个版本使用独立版本记录及固定 Git 标签；推送后必须从 GitHub 核对远端引用和内容。未通过远端备份核验，不得宣称该拟提交版本已准备完毕。
- 已发布标签和已评分结果不覆盖。重跑或修改形成新的制品，追加记录；成绩保留来源、重复评分与确切版本关联。
- 公开仓库不接收比赛图片、特征、线上逐图预测、凭证、运行缓存或未经核验的新模型载荷。受限原始结果保留在官方工作区，GitHub 保存摘要和恢复指针。
- 不强推、不改写历史、不更改仓库可见性。当前约定由 Agent 执行，不能声称已接入自动阻断上传的 Harness。
